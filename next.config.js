/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    runtime: 'nodejs'
  }
};

module.exports = nextConfig;

const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    runtime: 'nodejs'
  }
};

module.exports = nextConfig;
